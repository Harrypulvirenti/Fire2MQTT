package dev.harrypulvirenti.fire2mqtt.media

import android.service.notification.NotificationListenerService

/**
 * Stub NotificationListenerService required by MediaSessionManager.getActiveSessions().
 * The service itself does not need to do anything — its existence and user grant are sufficient.
 */
class MediaNotificationListener : NotificationListenerService()
